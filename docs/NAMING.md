# Naming Conventions

- Use the template name as the base for resource naming (via locals).
- Tags should include:
  - `project`
  - `environment`
  - `owner`
- Prefer lowercase, hyphenated names for AWS resources.
